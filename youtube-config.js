/*
 * Flux Websites configuration.
 *
 * Create a browser-restricted API key in Google Cloud with only the
 * YouTube Data API v3 enabled, then paste it between the quotes below.
 * Restrict the key to the exact domains where Flux is served.
 */
window.FLUX_YOUTUBE_API_KEY = '';
